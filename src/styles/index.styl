html,
body {
	margin: 0;
	padding: 0;
	overflow: hidden;
	font-family: Arial;
}

.legend,
.score {
	position: absolute;
	color: #fff;
}

.legend {
	padding-left: 15px;
	font-size: 18px;
}

.score {
	width: 100%;
	text-align: center
}